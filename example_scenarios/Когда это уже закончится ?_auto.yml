scenario_version: 1
scenario:
  name: "Когда это уже закончится ?"
  desc: " Когда это уже закончится ? "
  run_condition: "bool(re.compile('когда это уже закончится [а-яА-я]?').match(message.text.lower()))" # use only '' str notation
  nodes:
    start_node:
      text: "Никогда."
    